name: typeCheck

on:
  pull_request

jobs:
  typeCheck:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: volta-cli/action@v4
      - run: pnpm i --frozen-lockfile
      - run: pnpm typeCheck
